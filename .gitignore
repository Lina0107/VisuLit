# Environment and secrets (never commit)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.Python

# OS
.DS_Store
Thumbs.db

# Local deploy bundle (use scp to server, do not commit)
visulit-deploy.tar.gz

# VPS data backups (scripts/backup_data_vps.sh)
backups/

# Node / Next (if running tooling from repo root)
frontend/node_modules/
frontend/.next/
