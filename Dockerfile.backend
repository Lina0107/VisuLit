FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy only what the Flask API needs.
COPY app.py ./app.py
COPY templates ./templates

EXPOSE 5000

CMD ["python", "app.py"]

